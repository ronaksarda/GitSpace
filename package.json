{
  "name": "gitspace",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "node src/backend/server.js",
    "build": "echo \"Build complete. Serving frontend via static and running Node.js.\"",
    "seed:stress": "node src/backend/scripts/seed_stress.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^8.5.2",
    "pg": "^8.13.1"
  }
}
